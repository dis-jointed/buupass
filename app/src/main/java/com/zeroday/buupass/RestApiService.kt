package com.zeroday.buupass

import android.telecom.Call
import retrofit2.Callback
import retrofit2.Response

class RestApiService {
 /*   suspend fun addUser(userData: loginDetails, onResult: (loginDetails?) -> Unit){
        val retrofit = RetrofitInstance.buildService(LoginApi::class.java)
        retrofit.signInUser(userData).enqueue(
            object : Callback<loginDetails> {
                override fun onFailure(call: Call<loginDetails>, t: Throwable) {
                    onResult(null)
                }
                override fun onResponse( call: Call<loginDetails>, response: Response<loginDetails>) {
                    val addedUser = response.body()
                    onResult(addedUser)
                }
            }
        )
    }*/
}